{
  "name": "garli.click",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm run build -- --serve=8306",
    "build": "esbuild --bundle src/index.js --sourcemap --outfile=index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bski": "*",
    "esbuild": "*",
    "three": "*",
    "troika-three-text": "*"
  },
    "overrides": {
      "three": "$three"
    }
}
