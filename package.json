{
  "name": "bski.social",
  "version": "0.9.0",
  "private": true,
  "scripts": {
    "start": "npm run build -- --serve=8306 --servedir=.",
    "build": "esbuild --bundle src/index.js --sourcemap --outfile=index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Oleg Mihailik",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "ag-grid-community": "^33.0.4",
    "bski": "*",
    "eld": "^1.0.1",
    "esbuild": "*",
    "three": "*",
    "troika-three-text": "*",
    "wink-sentiment": "^5.0.2"
  },
  "overrides": {
    "three": "$three"
  }
}
